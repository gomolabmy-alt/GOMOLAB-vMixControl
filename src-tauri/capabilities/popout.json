{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popout",
  "description": "Capabilities for a popped-out canvas-page window (see Canvas.tsx's pop-out button / PopoutPage.tsx) — a sync client, not the host, so it needs baseline window/webview access plus explicit permission to close itself (Merge back / confirmed window close).",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["page-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "os:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
